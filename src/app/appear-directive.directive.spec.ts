import { AppearDirectiveDirective } from './appear-directive.directive';

describe('AppearDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AppearDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
